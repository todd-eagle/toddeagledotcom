import React from 'react'
import {MenuButton, MenuIcon} from '../../../styles/components/Menus'

const Menu = () => {
    return (
        <MenuButton>
            <MenuIcon></MenuIcon>
        </MenuButton>
    )
}
export default Menu
